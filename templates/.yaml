apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: "terss"
  namespace: argocd
spec:
  source:
    sourceRepos:
      - '*'
    destinations:
      - namespace: '*'
        server: '*'
    clusterResourceWhitelist:
      - group: '*'
        kind: '*'
    helm:
      parameters : "terss"

